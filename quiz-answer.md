1. What stack are we doing for this Unit
A: MEN Stack 

2. Node is a programming language (True or False)
A: False 

3. What is the protocol that the computers are using to send the requests and responses?
A: Hypertext transfer protocol (HTTP)

4. When we send a request from the client to the server what always happens next?
A: Get a response 

5. List at least 2 HTTP methods below
A: GET / PUT / Delete 


6. In your words describe to me what the server is and what the client is
A:the server is a machine to store Data and the client is a machine that requests data from the Server 

7. What is the difference between error messages that start with 400 vs ones with 500?
A: 400 client side and 500 server side 

8. What is the first step you always run to initialize node projects
A:npm init -y 

9. true or false? the code for packages we download is stored in the package.json?
A: True 

10. You cloned code from github and you see that there isn’t the node_modules folder but there is a package. json file with the dependencies. What command do you run to download all the dependencies?
A: npm install ( the package name )

